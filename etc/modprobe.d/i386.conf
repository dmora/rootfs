# Streamline module loading by disabling unused modules for i386 architecture
# alias parport_lowlevel parport_pc  # Disabled as parallel ports are not used
# alias char-major-10-144 nvram      # Disabled, assuming UEFI is used instead of BIOS
# alias binfmt-0064 binfmt_aout      # Disabled, no a.out binaries are necessary
# alias char-major-10-135 rtc        # Keep if hardware clock access is required
